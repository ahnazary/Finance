version: '3'

services:
  postgres:
    image: postgres:15
    environment:
      POSTGRES_DB: stocks     
      POSTGRES_USER: postgres         
      POSTGRES_PASSWORD: postgres 
    ports:
      - "5438:5432" 
    volumes:
      - postgres_data:/var/lib/postgresql/data

volumes:
  postgres_data:
